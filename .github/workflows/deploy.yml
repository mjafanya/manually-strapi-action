name: Deploy Strapi on EC2

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'
        cache: 'root/manually-strapi-action/my-project'

    - name: Install dependencies
      run: npm install
      working-directory: ./my-project

    - name: Build Strapi
      run: npm run build
      working-directory: ./my-project

    - name: Deploy to EC2
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        EC2_USER: 'ubuntu'
        EC2_HOST: 'your-ec2-public-ip'
        EC2_PORT: '22'
      run: |
        echo "$SSH_PRIVATE_KEY" > private_key.pem
        chmod 600 private_key.pem
        scp -i private_key.pem -r ./* $EC2_USER_EC2_HOST:/root/manually-strapi-action/my-project
        ssh -i private_key.pem $EC2_USER_EC2_HOST 'bash -s' <<'EOF'
          cd /root/manually-strapi-action/my-project
          npm install --production
          pm2 restart all || pm2 start npm --name "strapi" -- run start
        EOF

